
spring.application.name=backend
server.port=8081
# Activate the 'dev' profile locally
spring.profiles.active=dev

# Database Connection (Safe to share the URL)
spring.datasource.url=jdbc:postgresql://localhost:5432/lostandfound
spring.datasource.username=postgres
spring.datasource.password=Ragul

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
logging.level.org.springdoc=DEBUG
logging.level.org.springframework.web=DEBUG

# Serialize dates as ISO strings (e.g. "2026-03-10") not as arrays
spring.jackson.serialization.write-dates-as-timestamps=false

# ?? JWT ??????????????????????????????????????????????????????????????????????
# Secret must be >= 256 bits (32 chars) for HMAC-SHA256
app.jwt.secret=jsdyusdfyuewfyueyuweyfgyegfyebycbsdhgfeygfywegwy2736ggewuegywehue
# 24 hours in milliseconds
app.jwt.expiration-ms=86400000

